const {TestScheduler} = require("jest");


test("why does file get added to Users/rileycook/xmesh/contract-sepolia.json", () => {
    expect(1).toBe(1);
}
);

// const contract = require("../api/contract")

// test("test get_current_contract_config_json()", () => {
//     let contract_json = contract.get_current_contract_config_json()
//     console.log(contract_json)
// }
// );